
// Este arquivo é mantido para compatibilidade com o código existente
// Toda a lógica foi refatorada em hooks separados

import { useBibleData, useBibleFavorites } from './useBibleData';

export function useBibleData() {
  return useBibleData();
}

export function useBibleFavorites() {
  return useBibleFavorites();
}
