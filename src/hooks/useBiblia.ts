
// Este arquivo é mantido para compatibilidade com o código existente
// Toda a lógica foi refatorada em hooks separados

// Exportando os hooks separados em vez de redeclarar
export { useBibleData } from './useBibleData';
export { useBibleFavorites } from './bible/useBibleFavorites';
